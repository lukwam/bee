---
steps:
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk:alpine
    dir: functions/update_hints
    entrypoint: gcloud
    args:
      - functions
      - deploy
      - update_hints
      - --gen2
      - --project=${PROJECT_ID}
      - --region=us-central1
      - --runtime=python312
      - --service-account=bee-hints@lukwam-bee.iam.gserviceaccount.com
      - --set-secrets=ZYTE_API_KEY=zyte-api-key:latest
      - --timeout=300
      - --trigger-http
      - -q
